version: '3.8'

services:
  # MongoDB副本集自动配置服务，但是这里因为是在docker容器中，所以我们得到的是神奇网址
  # mongodb-config:
  #   image: alpine:latest
  #   container_name: novel-mongodb-config
  #   volumes:
  #     - ./scripts:/scripts:ro  # 挂载脚本目录
  #   command: >
  #     sh -c "
  #       echo '🔧 开始自动配置宿主机MongoDB副本集...' &&
  #       # 在宿主机上执行MongoDB配置脚本
  #       MONGO_ADMIN_USER='admin' MONGO_ADMIN_PASS='xxx' MONGO_PORT='27017' /scripts/config-host-mongodb.sh &&
  #       echo '✅ MongoDB副本集配置完成'
  #     "
  #   network_mode: host
  #   restart: "no"

  # Novel API 服务，这里的novel-api是服务名称，ok
  novel-api:
    build:
      context: .
      dockerfile: Dockerfile
    # 自动的image name会是projectName_containerName
    container_name: novel-api
    # | 策略             | 行为               |
    # |----------------|------------------|
    # | no             | 不自动重启（默认）        |
    # | on-failure     | 只有在容器退出码非0时才重启   |
    # | always         | 无论什么情况都重启，包括手动停止 |
    # | unless-stopped | 除非手动停止，否则总是重启    |
    restart: unless-stopped
    # 容器内部，全局环境变量 ${环境变量:-default}
    environment:
      - SERVER_PORT=8080
      - MONGODB_URI=mongodb://${MONGO_USER:-admin}:${MONGO_PASS:-password}@host.docker.internal:27017/novel?replicaSet=rs0&authSource=admin
      - MONGODB_DATABASE=novel
      - MONGODB_TIMEOUT=30s
      - MONGODB_MAX_POOL_SIZE=10
      - MONGODB_MIN_POOL_SIZE=2
      - FABRIC_CERT_PATH=/app/test-network/organizations/peerOrganizations/org1.example.com
      - FABRIC_PEER_HOST=peer0.org1.example.com
      - FABRIC_PEER_PORT=7051
    #宿主机对外的是8080:docker端口，可以理解为钥匙：锁
    ports:
      - "8080:8080"
    volumes:
      # 挂载Fabric证书文件
      - ../test-network:/app/test-network:ro
    extra_hosts:
      # 允许容器访问宿主机服务
      - "host.docker.internal:host-gateway"
    networks:
      # 自动连接到两个网络
      - novel-network
      - fabric_test
    # depends_on:
    #   mongodb-config:
    #     condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

networks:
  # 内部网络
  novel-network:
    driver: bridge
    ipam:
      config:
        - subnet: 192.168.200.0/24

  # 外部Fabric网络（需要预先存在）
  fabric_test:
    external: true